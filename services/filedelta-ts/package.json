{
  "name": "@grip-lab/filedelta",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node dist/tests/reassembler.test.js && node dist/tests/fixtures.test.js"
  }
}
